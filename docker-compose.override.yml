networks:
  cloudflared:
    external: true

services:
  headless-rust:
    networks:
      - cloudflared
    extra_hosts:
      - "host.docker.internal:host-gateway"

  headless-rust-headful:
    networks:
      - cloudflared
    extra_hosts:
      - "host.docker.internal:host-gateway"
