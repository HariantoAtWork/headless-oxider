Bump to 1.1.1. Headful/Xvfb never exposed a working :9382: the image lacked `xauth`, and `xvfb-run` as PID 1 hangs on SIGUSR1 so `headless-rust` never starts. Install `xauth`, start Xvfb from a compose entrypoint (not xvfb-run as PID 1), and document ports 9381/9382 plus not exporting host `HEADFUL=1` for the default service.

