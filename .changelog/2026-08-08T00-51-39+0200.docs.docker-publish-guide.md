# Document Docker publish and tagging

Add `docs/docker-publish.md` and link it from the README so git tags, Docker Hub versions, multi-arch builds, and when to republish are clear without relying on a separate wiki.
