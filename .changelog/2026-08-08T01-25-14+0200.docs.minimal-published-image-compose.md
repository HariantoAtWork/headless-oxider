# Document minimal published-image compose

Add a pull-only `docs/docker-compose.image.yml` (headless + optional headful/Xvfb profile) and guide so users can run Hub images with ports and shm_size mainly, relying on Dockerfile ENV defaults.
