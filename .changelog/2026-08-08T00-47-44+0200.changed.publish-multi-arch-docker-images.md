# Publish multi-arch Docker images

GitHub Actions now builds and pushes `linux/amd64` and `linux/arm64` in one multi-platform manifest via QEMU and Buildx, so the same tags work on Intel/AMD and ARM hosts.
